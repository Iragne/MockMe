var http = require('http');
var assert = require("assert");
var should = require('should');



var port = 4242;

var app = null;
var sessionCookie = null;

describe('API', function(){
	describe('Models', function(){
		before (function (done) {
			done();
		});
		it('should exist TBD', function (done) {
			done();
		});
	});
});